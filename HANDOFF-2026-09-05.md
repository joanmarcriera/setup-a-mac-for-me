# Handoff — 2026-09-05

## What changed
- Added `CLAUDE.md` (new file, 95 lines): purpose, key files, run/test flow, layout, conventions, gotchas.
- Added a short "Usage" section to `README.md` (rebuild-in-4-steps + develop-in-3-steps), leaving the
  existing "What Changed" / detailed sections intact.

## Verified
- Spot-checked both docs against the real repo: `scripts/generate_brew_artifacts.py` and
  `scripts/build_search_index.py` do implement `--check`; `.github/workflows/validate.yml` runs the
  exact four checks described; `scripts/verify-setup.sh` takes a bundle arg (default `workstation`)
  as documented. No inaccuracies found — no edits needed.
- Current branch is `feat/update-mac-macos-order` (not `main`); working tree otherwise clean apart
  from these doc files.

## Left to do
Nothing outstanding from this pass.
